# Banco de dados (Credenciais para o Docker)
DB_USER=
DB_PASSWORD=
DB_NAME=

#URL de conexão
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@localhost:5432/${DB_NAME}?schema=public"

#Servidor
JWT_SECRET=
PORT=